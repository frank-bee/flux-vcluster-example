apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: env-dev
  namespace: flux-system
spec:
  interval: 1m
  releaseName: env-dev
  targetNamespace: env-dev
  wait: true
  chart:
    spec:
      chart: vcluster-eks
      version: 0.18.1
      sourceRef:
        kind: HelmRepository
        name: loft-sh
  install:
    createNamespace: true
  values:
    syncer:
      extraArgs:
        - --tls-san=env-dev.env-dev.svc.cluster.local
        - --out-kube-config-server=https://env-dev.env-dev.svc.cluster.local
